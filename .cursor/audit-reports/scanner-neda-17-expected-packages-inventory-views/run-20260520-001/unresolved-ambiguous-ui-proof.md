# Unresolved / ambiguous UI proof

| Case | Label shown |
|------|-------------|
| Unresolved, no catalog | No product link yet |
| Ambiguous (merged EP ids) | Needs review via `OperatorProductLinkageMeta` |

Null `resolved_product_id` does not throw — `mergeExpectedPackageRowsProductLinkage` returns safe contract.
