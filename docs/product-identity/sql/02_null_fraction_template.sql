-- NEXT-PRODUCT-ID-02 — Template: null fraction for resolved_product_id (read-only).
-- Uncomment ONE block at a time; run in Supabase SQL editor.

-- SELECT
--   count(*) AS total_rows,
--   count(*) FILTER (WHERE resolved_product_id IS NULL) AS null_resolved_product_id
-- FROM public.amazon_fba_inventory;

-- SELECT
--   count(*) AS total_rows,
--   count(*) FILTER (WHERE resolved_product_id IS NULL) AS null_resolved_product_id
-- FROM public.amazon_amazon_fulfilled_inventory;

-- SELECT
--   count(*) AS total_rows,
--   count(*) FILTER (WHERE resolved_product_id IS NULL) AS null_resolved_product_id
-- FROM public.amazon_inventory_ledger;

-- SELECT
--   count(*) AS total_rows,
--   count(*) FILTER (WHERE resolved_product_id IS NULL) AS null_resolved_product_id
-- FROM public.claim_candidates;
