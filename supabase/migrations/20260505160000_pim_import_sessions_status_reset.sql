-- OBSOLETE: This migration targeted `public.pim_import_sessions`, which is not deployed in all environments.
-- PIM Quick Import uses `raw_report_uploads` + metadata only. Left as a no-op for migration ordering.

BEGIN;

COMMIT;
